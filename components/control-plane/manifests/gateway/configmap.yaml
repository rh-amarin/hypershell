---
apiVersion: v1
kind: ConfigMap
metadata:
  name: openshell-gateway-config
  namespace: NAMESPACE_PLACEHOLDER
  labels:
    app.kubernetes.io/name: openshell
    app.kubernetes.io/component: gateway
    app.kubernetes.io/managed-by: hypershell-control-plane
    hypershell.redhat.io/managed: "true"
data:
  gateway.toml: |
    [openshell]
    version = 1

    [openshell.gateway]
    bind_address          = "0.0.0.0:8080"
    health_bind_address   = "0.0.0.0:8081"
    metrics_bind_address  = "0.0.0.0:9090"
    log_level             = "info"
    sandbox_namespace     = "NAMESPACE_PLACEHOLDER"
    default_image         = "SANDBOX_IMAGE_PLACEHOLDER"
    supervisor_image      = "SUPERVISOR_IMAGE_PLACEHOLDER"
    # Provisions client TLS material into sandbox runners so they can verify the
    # gateway's TLS server cert (sets OPENSHELL_TLS_CA from the secret's ca.crt).
    # Required by openshell 0.0.113's Combined topology; NOT external-client mTLS
    # (external clients authenticate via OIDC over the Route -- see the absence of
    # client_ca_path under [openshell.gateway.tls]).
    client_tls_secret_name = "openshell-client-tls"
    enable_loopback_service_http = true
    policy_validation_failure_mode = "fail_closed"
    server_sans = ["openshell-gateway.NAMESPACE_PLACEHOLDER.svc.cluster.local"]

    [openshell.gateway.tls]
    cert_path             = "/etc/openshell-tls/server/tls.crt"
    key_path              = "/etc/openshell-tls/server/tls.key"

    [openshell.gateway.credential_storage]
    key_encryption_key_env = "OPENSHELL_GATEWAY_CREDENTIAL_KEY_ENCRYPTION_KEY"

    [openshell.gateway.auth]
    allow_unauthenticated_users = false

    [openshell.gateway.gateway_jwt]
    signing_key_path = "/etc/openshell-jwt/signing.pem"
    public_key_path  = "/etc/openshell-jwt/public.pem"
    kid_path         = "/etc/openshell-jwt/kid"
    gateway_id       = "openshell-gateway"
    ttl_secs         = 3600

    [openshell.drivers.kubernetes]
    grpc_endpoint                = "https://openshell-gateway.NAMESPACE_PLACEHOLDER.svc.cluster.local:8080"
    service_account_name         = "openshell-gateway-sandbox"
    supervisor_sideload_method   = "image-volume"
    sa_token_ttl_secs            = 3600
    app_armor_profile            = "Unconfined"
    topology                     = "combined"

    [openshell.drivers.kubernetes.sidecar]
    proxy_uid                    = 1337
    process_binary_aware_network_policy = false
